<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <%- include('./links'); %>
  <title>Game</title>
</head>
<body>
    <%- include('./navbar'); %>
    <div class="container">
      <div class="mt-4 row justify-content-center">
        <div class="col mb-2">
          <h1 class="text-center">Vos résultats</h1>
        </div>
      </div>
      <div class="row justify-content-center mt-4">
        <h5>Votre score générale est de <%= stat %> % </h5>
      </div>
      <div class="row justify-content-center mt-4">
        <h5>Vos dernière parties : </h5>
      </div>
      <% if(games != "") { %>
        <div class="row mt-3">
          <% for(var i=0; i < games.length; i++) { %>
          <div class="col-sm-3 ">
            <p><b><%= games[i].name %></b>
            <br>Score : <%= games[i].finalScore %>/<%= games[i].numberOfQuestions %>
            <br>Date : <%= games[i].date %></p>
          </div>
          <% } %>
        </div>
      <% } %>
    </div>
      

</body>
</html>

