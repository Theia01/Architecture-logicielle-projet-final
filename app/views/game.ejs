<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <%- include('./links'); %>
  <title>Game</title>
</head>
<body>
    <%- include('./navbar'); %>
    <div class="container">
        <div class="mt-4 row justify-content-center">
          <div class="col mb-2">
            <h1 class="text-center">Prêt pour vous tester ?</h1>
          </div>
        </div>
        <form action="game" method="POST">
          <div class="row justify-content-center">
            <div class="">
              <button type="submit" class="btn btn-primary btn-lg mt-2">Débuter le quizz</button>
            </div>
            <div class="form-group">
              <span style="color : #dc3545;" class="form-text"><%=locals.message %></span>
            </div>
          </div>
          <div class="row justify-content-center">
            <div class="col-sm-7">
              <p class="form-text mt-5">Vous pouvez rajouter des options :</p>

              <% if(theme != "") { %>
              <div class="form-group">
                <label for="selectTheme">Thème</label>
                <select name="theme" class="form-control" id="selectTheme">
                  <option value="<%= theme[0].id %>" selected><%= theme[0].name %></option>
                    <% for(var i=1; i < theme.length; i++) { %>
                    <option style="text-transform: capitalize;" value="<%= theme[i].id %>"><%= theme[i].name %></option>
                    <% } %>
                </select>
              </div>
              <% } %>

              <div class="form-group">
                <label for="selectTheme">Difficulté</label>
                <select name="difficulty" class="form-control" id="selectTheme">
                  <option value="" selected>Choisir une difficulté</option>
                    <% for(var i=0; i < difficulty.length; i++) { %>
                    <option style="text-transform: capitalize;" value="<%= difficulty[i] %>"><%= difficulty[i] %></option>
                    <% } %>
                </select>
              </div>
              <div class="form-group">
                <label for="selectTheme">Nombre de questions</label>
                <select name="number" class="form-control" id="selectTheme">
                    <option value="5">5</option>
                    <option selected value="10">10</option>
                    <option value="15">15</option>
                    <option value="20">20</option>
                    <option value="30">30</option>
                </select>
              </div>
            </div>
          </div>
        </form>
      </div>

</body>
</html>

